.ipynb_checkpoints
*junk*
__pycache__
*.py[cod]
venv
input
dafs

# The documents are generated by `make docs` and published by the CI server.
docs

# Which steps have run, which is true of this machine rather than of the repository.
steps/.stamp

# The document tool chain, built by `make ci_env` out of the pinned `requirements.txt`.
.ci_env
